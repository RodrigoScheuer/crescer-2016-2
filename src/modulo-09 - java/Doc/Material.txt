gitbook
https://cwisoftware.gitbooks.io/projetocrescer/content/java/java.html#date

ctrl + shift + L  = extrair conteudo para variavel.

shift + alt + s = gerar (construtor, getterSetter).

sout = system.out.println

psvm = main

alt + seta = move linha de codigo.

        <dependency>
            <groupId>com.oracle</groupId>
            <artifactId>ojdbc6</artifactId>
            <version>11.2.0.3</version>
        </dependency>